{% extends "layout.njk" %}

{% block content %}
    <section class="anuncio">
        <div class="title-anuncio">
            <h1>As melhores receitas</h1>
        </div>
        <div class="content-anuncio">
            <p>Aprenda a construir os melhores pratos. </p>
            <p>Receitas criadas por</p> 
            <p>profissionais do mundo inteiro</p>    
        </div>
        <div class="img-anuncio">
            <img src="assets/chef.png" alt="chef">
        </div>            
    </section>

    <div class="title">
        <h1>Mais acessadas</h1>
    </div>

    <div class="cards">
        {% for item in itens %}  
            <div class="card" id="{{item.title}}">
                <div class="card-image">
                    <img src="{{item.image}}" alt="{{item.title}}">
                </div>
                <div class="card-title">
                    <p id="{{item.id}}">{{item.title}}</p>
                </div>
                <div class="card-person">
                    <p>{{item.author}}</p>
                </div>
            </div>
        {% endfor %}
    </div>
{%endblock%}}

{% block scripts %}
    <script src="js/script.js"></script>
{% endblock %}